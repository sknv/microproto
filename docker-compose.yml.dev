version: '3.5'

services:
  consul:
    image: consul:1.2.1
    command: agent -client 0.0.0.0 -dev -ui
    hostname: node1
    ports:
      - 8300:8300 # rpc port
      - 8500:8500 # http api port
      - 8600:8600 # dns server port
    restart: unless-stopped
